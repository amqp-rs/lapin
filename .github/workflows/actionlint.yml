name: actionlint

on:
  push:
    branches:
      - main
    tags:
      - "**"
  pull_request:

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - uses: amqp-rs/actions/checkout@main
      - uses: amqp-rs/actions/actionlint@main
